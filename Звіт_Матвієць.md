## Опис виконаної роботи

У ході практичної роботи було розгорнуто та налаштовано MQTT-брокер для обміну повідомленнями.

### 1. Встановлення та конфігурація брокера
Для роботи використано **Eclipse Mosquitto**. У файлі конфігурації `mosquitto.conf` активовано прослуховування порту 1883 та дозволено анонімні підключення для тестування:

```conf
listener 1883
allow_anonymous true

<img width="621" height="583" alt="image" src="https://github.com/user-attachments/assets/0a684ac0-c5ee-4857-955b-d5a2532bb596" />

Перевірка працездатності через CLI
За допомогою PowerShell виконано підписку на топік та публікацію повідомлень. Це підтверджує, що брокер коректно обробляє запити всередині системи.

